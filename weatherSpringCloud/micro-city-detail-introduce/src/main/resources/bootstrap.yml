spring:
  cloud:
    config:
      name: micro-city-detail-introduce
      profile: dev
      label: master
      uri: http://localhost:8776/
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mysql?characterEncoding=utf-8&useSSL=false
    username: root
    password: 420188
mybatis:
  mapper-locations: classpath:mapper/*.xml